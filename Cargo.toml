[package]
name = "audio-capture-rs-talaroana"
version = "0.1.0"
edition = "2021"
description = "Zero-dependency cross-platform audio capture library"
license = "BlueOak-1.0.0"

[dependencies]

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "capture_bench"
harness = false
libc = "0.2"

[target.'cfg(target_os = "macos")'.dependencies]
coreaudio-sys = "0.2"
